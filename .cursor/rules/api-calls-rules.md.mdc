---
description: Esta regra é útil em tasks que envolvem chamadas autenticadas à API
alwaysApply: false
---
# Regras de chamadas de API

## 📄 `hooks/useAuthenticatedRequest.ts`

- **Sempre use `execute` do hook `useAuthenticatedRequest` para chamadas autenticadas à API.**
  - Isso garante tratamento de erros, loading, e exibição de notificações de forma consistente.
  - Evite usar `fetch` ou `axios` diretamente para requisições protegidas.# Regras de chamadas de API

## 📄 `hooks/useAuthenticatedRequest.ts`

- **Sempre use `execute` do hook `useAuthenticatedRequest` para chamadas autenticadas à API.**
  - Isso garante tratamento de erros, loading, e exibição de notificações de forma consistente.
  - Evite usar `fetch` ou `axios` diretamente para requisições protegidas.